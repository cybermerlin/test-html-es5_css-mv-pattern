//  es5   !!!

/**
 * @class Test.block.View
 * @constructor
 */
function View(cfg) {
  cfg = cfg || {};
  this.el = cfg.el || document.getElementsByTagName('article')[0];
}

View.prototype.add = function() {
  var tpl = '<section>'+'</section>';
  
};
